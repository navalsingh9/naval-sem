# NAVAL-SEM — full dependency list (offline-packaged via PyInstaller)

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.47.0
python-multipart>=0.0.29
pydantic>=2.13.4

# Statistical core
semopy>=2.3.11
pandas>=3.0.3
numpy>=1.26.0
scipy>=1.12.0

# File format support
openpyxl>=3.1.5
xlrd>=2.0.1
pyreadstat>=1.3.5

# Native desktop window
pywebview>=6.2.1

# Build / packaging tools (install in dev env, not bundled)
pyinstaller>=6.20.0
